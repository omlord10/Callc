#include "evaluator.h"
#include <string.h>
#include <stdio.h>
#include <stdlib.h>

/*int evaluate_expression(const char *input, double *result)
{
    int status = Error_Evaluator_Evaluate_None;
    double res = 0.0;

    if (calculate(input, &res) != 0)
    {
        printf("Error: evaluation failed\n");
        return Error_Evaluator_Evaluate_CalcFailed;
    }

    *result = res;
    return status;
}
*/
